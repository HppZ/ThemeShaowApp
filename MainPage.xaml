<Page
    x:Class="App2.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App2"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid >
        <Grid x:Name="Grid2" Grid.RowSpan="2" ></Grid>
        <Grid x:Name="Grid1" Width="60" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.Shadow>
                <ThemeShadow x:Name="Shadow1"></ThemeShadow>
            </Grid.Shadow>
            <Border Grid.Row="1" BorderThickness="1" Background="White"  Margin="0 -2 0 0" Height="80"></Border>
            <Polygon Grid.Row="0"
                HorizontalAlignment="Center"
                Fill="White"
                StrokeThickness="1"
                Points="0,10 10,0 20,10"/>
            <Border Grid.Row="1"  Margin="1 -1 1 0" VerticalAlignment="Top" BorderThickness="1" Height="1" BorderBrush="White"></Border>
        </Grid>
    </Grid>
</Page>
